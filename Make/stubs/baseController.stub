<?php

namespace App\{{ FixServiceName }}\Controllers;

use Illuminate\Support\Facades\DB;
use Illuminate\Support\Facades\Log;
use Core\CoreController;

class BaseController extends CoreController
{
    // 服务拆分 或 每个模块单独配置的时候，可以在这里配置
    /*public function __construct()
    {
        parent::__construct();

        // 设置默认的数据库驱动
        DB::setDefaultConnection('clue');
        // 设置默认的日志驱动
        Log::setDefaultDriver('clue_daily');
    }*/
}
